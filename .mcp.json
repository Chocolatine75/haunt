{
  "mcpServers": {
    "haunt": {
      "command": "sh",
      "args": ["${PLUGIN_DIR}/mcp-server/start.sh"],
      "env": {
        "HAUNT_PERSONAS_DIR": "${PLUGIN_DIR}/personas",
        "ANTHROPIC_API_KEY": "${ANTHROPIC_API_KEY}"
      }
    }
  }
}
