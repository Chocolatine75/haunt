{
  "mcpServers": {
    "haunt": {
      "command": "sh",
      "args": ["${CLAUDE_PLUGIN_ROOT}/mcp-server/start.sh"],
      "env": {
        "HAUNT_PERSONAS_DIR": "${CLAUDE_PLUGIN_ROOT}/personas"
      }
    }
  }
}
